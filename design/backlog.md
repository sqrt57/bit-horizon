# Backlog

Open questions and tasks not yet done. Resolved decisions and completed work have
moved to [changelog.md](changelog.md).

## Open questions

- **Manual Scan yield** — the GDD's Executive Core zone specifies a "Manual Scan" tap action but not how much it should grant. Currently a placeholder `MANUAL_SCAN_YIELD = 1.0` Data Bit (`GameState.kt`), matching the "+1.0" base-output convention used for every tier's passive production. Needs a real balance pass once the early-game pacing is played end-to-end.

## Tasks

_None currently._
